@model IEnumerable<MVCDatatransactionBD.Pais>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pai_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pai_codigointernacional)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pai_predeterminado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pai_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pai_codigointernacional)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pai_predeterminado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pai_codigo }) |
            @Html.ActionLink("Details", "Details", new { id=item.pai_codigo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pai_codigo })
        </td>
    </tr>
}

</table>
